## やること

`/admin` の「アクセス推移」セクションに **月別** タブを追加する。

## 変更内容

`src/components/admin/ComparisonAnalytics.tsx` のみ変更。

1. 集計ロジックに `monthlyStats` を追加
   - JST基準で `YYYY-MM` にグルーピング
   - 既存の週別/曜日別と同じ形（total / interacted / cvr）で算出
2. Tabs に「月別」を追加（現状: 日別 / 週別 / 曜日別 / 時間帯別 / クロス分析 → 月別を週別と曜日別の間に配置）
3. 既存タブと同じ複合グラフ（棒: 件数、直線折れ線: CVR%、右軸0-100%）で描画
4. X軸ラベルは `YYYY/MM` 形式

## スコープ外

- 他タブの変更なし
- DB/ロガー変更なし
- クロス分析への月次軸追加は今回なし
